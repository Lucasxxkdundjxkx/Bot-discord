{
  "name": "workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter=!@workspace/discord-bot run typecheck"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
